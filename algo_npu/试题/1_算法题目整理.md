# 算法题目整理

## 选择题（每题2分）

1. 采用分治法解决问题时，算法时间复杂度分析典型的递推方程为 T(n) = aT(n/b) + f(n)，对于某一个问题，若 a = 2，b = 2，且 f(n) = O(n)，则求该问题的时间复杂度为（ ）
2. （多选）以下哪个问题适合用贪心算法求解（ ）
   - 0-1背包问题
   - 哈夫曼编码
   - 单源最短路径
   - 最优装载问题
4. 适合用回溯法和分支定界法求解的问题均需要满足（ ）
   - 重叠子问题
   - 最优子结构性质
   - 多米诺性质
   - 独立子问题
5. 以下哪个问题的解空间树是排列树（ ）
   - n皇后问题
   - 最大团问题
   - 批作业调度问题
   - 图的m着色问题
5. 以下关于渐进符号性质表达正确的是（ ）
7. 回溯法一般是按（ ）策略搜索问题的解空间树
   - 深度优先
   - 广度优先
   - 广度深度结合
   - 活结点优先
8. 适合应用贪心算法与动态规划算法求解的问题所具有的共同特点是（ ）
   - 重叠子问题
   - 最优子结构性质
   - 贪心选择性质
   - 独立子问题
9. 对于物品数量为n，背包容量为c的情况下的0-1背包问题，若用回溯法求解，算法的时间复杂度为（ ）

## 填空题（每空2分）

1. 算法是由若干指令组成的有穷序列，需要满足____、____、____、____、____。

2. 算法的效率一般可用从两个方面衡量，分别称为____和____。

3. 假设算法A理论上的时间复杂度T(n) = n³，现在有两台同类型计算机M1和M2。M2的计算速度是M1的64倍。若在M1和M2上分别测试算法A，则在相同的时间内，M1和M2能够求解的问题规模n1和n2的关系为____。

4. 若算法B理论上的时间复杂度为T(n) = n²，若在M1和M2上分别测试算法B，则M1和M2求解相同规模问题所耗费的时间t1和t2的关系为____。

5. 用回溯法求解问题时，可将问题解空间看作一棵树状结构，根据问题不同，解空间树一般可分为子集树和排列树两种结构，对于N皇后问题，其解空间树是____结构。对于TSP问题，其解空间树是____结构。

6. 用分治法求解n个元素的全排列问题时，算法时间复杂度递推方程为____。

7. 0-1背包问题可用适用动态规划、回溯和分支定界三种方法求解，其中排序对求解过程没有影响的方法是____。

8. 若序列X = [A, B, C, B, D, A, B]，Y = [B, D, C, A, B, A]，则X和Y的一个最长公共子序列为____；采用动态规划方法求解该类问题（序列X和Y的规模分别为m和n）的时间复杂度为____。

9. 采用分治法解决问题时，算法时间复杂度分析典型的递推方程为T(n) = aT(n/b) + f(n)，对于某一问题，若a = 2，b = 2，且f(n) = O(n)，则求解该问题的时间复杂度为____；若a = 2，b = 2，且f(n) = O(1)，则求解该问题的时间复杂度为____；若a = 2，b = 2，且f(n) = O(n²)，则求解该问题的时间复杂度为____；若a = 2，b = 2，且f(n) = O(n^log_b a)，若希望将算法时间复杂度降低为不超过线性情况，则a和b应该满足的条件为____。

10. 应用贪心算法求解问题的关键在于____。

11. 动态规划算法的求解步骤有四步，分别为____、____、____、____。

12. **对于有n个顶点，m条边的连通带权图，如果用Kruskal算法求其最小生成树，则时间复杂度为；如果用Prim算法求其最小生成树，则时间复杂度为。**

13. 遗传算法主要借鉴的是生物界的____和____，**遗传操作有、和。**

14. **遗传算法评价一个解的好坏用，其设计标准是。**

15. **基本遗传算法主要由4部分组成分别是、、、。**

16. 分支定界法主要有两种搜索方式，分别为____和____。

17. 节点v的上界UB(v)是指从v出发得到的所有叶子节点的效益值均____UB(v)，如果所有叶子节点的最小效益值等于节点v的下界LB(v)，则LB(v)为节点v的____。

18. 分支限界法求解极小化问题时，主要用节点的____进行剪枝。

19. 回溯法一般按照深度优先的策略搜索解空间树，**在搜索的过程中主要通过**  和  **避免无效的搜索**。

## 算法设计与分析题（每题10分）

1. 设B为数组，B[1…n]中存储了n个元素，分析下面的算法并回答问题。

```算法
ALG(B, left, right):
    if left = right then return B[left]
    mid = (left + right) / 2
    max1 = ALG(B, left, mid)
    max2 = ALG(B, mid+1, right)
    if max1 >= max2 then return max1 else return max2
```

（1）该算法的输出是什么？
（2）以比较作为基本操作，给出算法ALG的时间复杂度递推方程，并求解算法ALG的时间复杂度。
（3）对于求解该问题，算法ALG效率是否已最高，为什么？

2. 有如下图所示的地图，图中有A-K共11个地点，两点之间的路径长度为边中的数字，如A到B的路径为3。求A到K的最短路径？

（1）若用动态规划算法求解该问题，请给出最短路径的递推定义
（2）针对上图，按照你所给出的递推定义，逐步求解从A走到K的最短路径

3. 有形如下的数塔，若要求从数塔顶层出发，每个节点可以选择向左走或向右走，一直走到塔底，使得走过路径上节点的数值之和最大，回答以下问题：

```
      9
    12 15
  10  6   8
2   18  9   5
```

（1）若用动态规划算法求解该问题，请给出最优值的递推定义，包括初值说明
（2）针对上图的数塔，按照你所给出的递推定义，逐步求解从塔顶走到塔底的最大数值之和与得到最大数值和的路径

4. 已知一个国家有n个城市，已知每个城市过去一年的GDP总量，请找出其中GDP总量最接近的两个城市，回答以下问题：

（1）用数学方法描述该问题的求解目标
（2）为该问题设计一个有效算法，需要给出算法思想和伪代码
（3）对你所设计的算法进行时间复杂度分析
（4）对于求解该问题，你所设计的算法的效率是否已最高，为什么？

5. 已知有n个学生，若要求找出其中体重最为接近的两位学生，回答以下问题：

（1）用数学方法描述该问题求解目标
（2）为该问题设计一个有效算法，需要给出算法思想和伪代码
（3）对你所设计的算法进行时间复杂度分析

## 算法设计与实现（每题分值不定，大概10+）

1. 工作分配问题：有n项工作分配给n个人，将工作i分配给第j个人所需的费用为c[i][j]，设计一个算法，计算最佳工作分配方案，使得总费用最小。

要求：
（1）分析该问题适合用何种算法求解
（2）给出问题解的形式
（3）给出求解问题的代码框架
（4）分析算法的时间复杂度

2. 某集团领导年底要到该集团的各个分部视察工作。已知该集团共有n个分部。给定n阶矩阵C，其中C[i][j]表示领导从分部i到分部j所需要花费的代价。如果分部i到分部j不可达，则C[i][j] = ∞。求从分部1出发，视察每个分部一次后，再回到分部1所需的最少花费？

若用优先队列式分支限界法求解该问题，回答以下问题：
（1）每个搜索节点需要包含哪些信息，为什么？
（2）给出部分解的下界函数，越大越好
（3）给出部分解的上界函数，越小越好

3. 设某游艇俱乐部在长江沿岸设置了n个游艇出租站，1, 2, …, n。游客可在这些游艇出租站租用游艇，并在任何另一个游艇出租站归还游艇。已知游艇出租站i到游艇出租站j之间的租金为r[i][j]。若要计算从游艇出租站1到游艇出租站n所需的最少花费，完成以下问题。

（1）若用动态规划方法求解该问题，给出问题解的递推定义。
（2）填空完成以下动态规划算法程序。

```java
/**
 * @param n 游艇租赁站数量
 * @param r 租赁站之间的租赁费用矩阵
 */
public int minCost(int n, int[][] r) {
    // 填空
}
```